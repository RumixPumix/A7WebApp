import { ExpectedIssue } from '../../../utils/expectedIssue.js';
import errorWrapper from '../../../utils/errorWrapper.js';

function unbanUser(userId) {
    return true; // Simulate a successful unban operation
}

export default unbanUser;